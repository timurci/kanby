# Copy this file to '.env.compose' and set your desired credentials.

# These variables configure the PostgreSQL database container (db)
# and are used by the API container to build the SESSION_SERVICE_URI.
POSTGRES_USER=kanby_user
POSTGRES_PASSWORD=secure_dev_pass
POSTGRES_DB=kanby_db